
New/Init - use a map[string]string - and look for "debug_*" in it, if find "debug":map[string]string then use all of it
	Match a sst of flags to it to turn on/off
	If NONE - then a global "off" flag

